# Atlas Infra Notes

Terraform manages the stable Atlas foundation here.

- project
- cluster
- optional database users

Temporary laptop IP access should be added with Atlas CLI, not Terraform.

Example:

```bash
./connect-atlas.sh YOUR_PROJECT_ID
```
